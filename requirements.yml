---
# Ansible Galaxy requirements
# Install with: ansible-galaxy install -r requirements.yml

collections:
  - community.general
  - ansible.posix
  - kubernetes.core  # Required for monitoring stack (k8s, k8s_info modules)
